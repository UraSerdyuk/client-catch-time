// @ts-ignore-start
const defaultState = {
    stopWatch:{},
    isTimeOn:false
}

export default function stopWatchReducer(state=defaultState , action: any) {
    switch (action.type) {
        default:
   return  state

    }
}
